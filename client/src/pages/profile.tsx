import Link from 'next/link';
import Header from '../components/Header';

const Profile = () => {
  return (
    <div>
      <Header />
      <h2>Profile</h2>
    </div>
  );
};

export default Profile;
